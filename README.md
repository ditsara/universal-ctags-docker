universal-ctags-docker
======================

Run [Universal CTags](https://github.com/universal-ctags/ctags) from a docker
container.

- Download / copy `uctags` to `/usr/local/bin` or a directory in your path
- `chmod +x uctags`
